# DXFER Bug And Feature Log

Use this log for bugs and feature requests that arrive while another task is active. Do not switch away from active work unless the new input directly affects that work. Mark an item complete only after it is coded and verified in the running app.

## Open

- [ ] UT-SOLVER-001: User-tested regression: dragging entities with dimensions is not robust, can break the solver, and deleting the dimension can leave the solve broken.
- [ ] UT-SOLVER-002: User-tested regression: coincident constraints are not welding vertices together as expected; dragging a dimension can rerender the shape unbroken afterward.
- [ ] UT-ELLIPSE-001: User-tested regression: keyed-in ellipses create broken solves.
- [ ] UT-SOLVER-003: User-tested regression: after deleting a vertical constraint, dragging the unconstrained end of a dimensioned line works, but dragging the associated dimension snaps the line back vertical incoherently.
- [ ] UT-UI-001: User-tested request: left/right docked toolbar panels need slightly wider dock width, no horizontal scrollbar, and collapsible behavior.
- [ ] UT-ICON-001: User-tested icon pass: draw icons about 25% larger and remove hotkey labels from icon buttons because labels block the icon.
- [ ] UT-ICON-002: User-tested icon fixes: construction should be a diagonal dotted line; cut-at-point should be scissors; scale should be filled small square plus diagonal double-arrow to larger unfilled square; pattern should be a 2x2 square grid with top-left filled; circular pattern should be a hexagon of dots with one filled; rotate icons should use standard rotate arrows; text icon should be a capital A.
- [ ] Complete Stage 7 larger audit gaps using the targeted breakdown in `docs/superpowers/plans/2026-05-06-dxfer-stage7-architecture-gap-plan.md`.

## Done

- [x] UT-LINEARC-001 line/tangent-arc chain preview and keyed dimensions: made chained line endpoint hover toggle robust even when the canvas does not return a point target, changed invalid tangent-arc fallback preview to draw the start-to-end chord instead of the tangent-control segment, suppressed the tangent-control point from preview vertex markers, and added tangent-arc radius key-in handling so typed radius values create persistent radius dimensions. Focused JS canvas tests passed 149/149, keyed arc dimension factory tests rebuilt and passed 3/3 including tangent arc, `DXFER.slnx` rebuilt cleanly, and the rebuilt app verified a toolbar-created line toggled to `tangentarc`, the old mirrored-segment sample at `(6,0)` had `0` yellow pixels, the tangent-control marker sample at `(8,0)` had `0` yellow pixels, the arc preview had yellow pixels on the actual arc, and pressing Enter after typing radius `5` produced a persistent `R5` radius input. User retest remains tracked separately in `docs/dev/user-test-tracking.md`.
- [x] UT-SPLINE-001 spline fit/tangent handle editing: exposed spline `controlPoints` to the canvas DTO, rendered persistent fit/control edit points, made fit points and endpoint tangent handles first-class selectable point targets, added preview/server drag handling for fit/control points and tangent-start/tangent-end handles, and added whole-spline translation support. Focused JS tests for spline target selection and preview dragging passed, focused core drag tests passed, `SketchGeometryDragServiceTests|CanvasDocumentDtoTests` passed 28/28, `DXFER.slnx` rebuilt cleanly, and the rebuilt app verified a fresh toolbar-created spline: fit-1 hovered at `(4,0)`, dragged to `(4,2)`, tangent-start hovered at `(1,0.5)`, dragged to `(1,1)`, and the adjacent fit point then hovered at `(4,4)` with the old `(4,2)` target gone. User retest remains tracked separately in `docs/dev/user-test-tracking.md`.
- [x] UT-TRIM-004 arc extend to ellipse-family boundaries: added a failing large-angular-span regression for an arc ending at 40 degrees extending to a full ellipse boundary at 300 degrees, then fixed extension-boundary parameter collection so both valid before-start and after-end unwraps are retained. Focused large-span test passed, broader Power Trim arc/ellipse extend tests passed 128/128, `drawingCanvasSelection.test.mjs` passed 141/141, `DXFER.slnx` rebuilt cleanly, and the rebuilt app verified a toolbar-created center-point arc plus ellipse boundary: hover reported `powerTrimMode=extend`, click returned `OnPowerTrimRequested:ok`, entity count stayed `2`, and a 250-degree probe hovered the extended arc. User retest remains tracked separately in `docs/dev/user-test-tracking.md`.
- [x] UT-TRIM-001 polygon trim explosion: user-tested regression reproduced in core with stale polygon sketch dimensions/constraints; polygon target trim now removes the polygon, emits kept perimeter spans as individual `LineEntity` records, and filters dimensions/constraints referencing the deleted polygon id. Focused polygon trim tests passed 10/10, broader Power Trim tests passed 127/127, `DXFER.slnx` rebuilt cleanly, and the rebuilt app canvas verified toolbar-created polygon targets trim into `line:polygon-line-*` entities for line and circle cutter cases with no red/stale dimension inputs. User retest remains tracked separately in `docs/dev/user-test-tracking.md`.
- [x] UT-TRIM-002 extend hover tolerance and indicator: added Power Trim request-mode classification, widened off-end open-path hover/click tolerance to 18 px for extend candidates, added `data-power-trim-mode`, and drew a cyan dashed endpoint-to-pointer extension cue. `drawingCanvasSelection.test.mjs` passed 141/141, `DXFER.slnx` rebuilt cleanly with the canvas module cache-buster bumped, and the rebuilt app verified a line target hovering 14 px past its endpoint reported `powerTrimMode=extend`, painted cyan indicator pixels, and extended to the boundary with entity count stable. User retest remains tracked separately in `docs/dev/user-test-tracking.md`.
- [x] UT-TRIM-003 spline trim responsiveness: cached immutable spline sample paths and made degree-1 spline entities use their control points directly instead of oversampling to 257 points. New `SplineEntityTests` passed 2/2, spline Power Trim precision tests passed 19/19, `drawingCanvasSelection.test.mjs` passed 141/141, `DXFER.slnx` rebuilt cleanly, and the rebuilt app verified toolbar-created spline trim completed in about 174 ms, changed entity count `3 -> 4`, removed the picked span from hover, left both kept spans hoverable, and reduced the first trimmed span from 257 to 27 exposed points. User retest remains tracked separately in `docs/dev/user-test-tracking.md`.
- [x] Stage 7G raw target-key canvas module split: added `wwwroot/canvas/targetKeys.js`, moved shared point/segment/constraint key separators plus raw point and segment key parsing out of `drawingCanvas.js`, kept current-entity resolution in the facade, and added `canvasTargetKeys.test.mjs`. JS target-key/parsing/input-state/presentation/target/dimension/geometry/canvas/dock suites passed 174/174, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the restarted app loaded one nonblank `1098 x 718` canvas with no fresh console/page errors.
- [x] Stage 7G dimension-input-parsing canvas module split: added `wwwroot/canvas/dimensionInputParsing.js`, moved persistent dimension commit parsing and positive-value commit policy out of `drawingCanvas.js`, kept the existing facade re-export stable, and added `canvasDimensionInputParsing.test.mjs`. JS parsing/input-state/presentation/target/dimension/geometry/canvas/dock suites passed 171/171, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the restarted app loaded one nonblank `1098 x 718` canvas with no fresh console/page errors.
- [x] Stage 7G dimension-input-state canvas module split: added `wwwroot/canvas/dimensionInputState.js`, moved active-key selection, pending-focus resolution, input cycling, visible descriptor ordering, dimension-key extraction, and new persistent dimension detection out of `drawingCanvas.js`, kept existing facade re-exports stable, and added `canvasDimensionInputState.test.mjs`. JS input-state/presentation/target/dimension/geometry/canvas/dock suites passed 169/169, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the restarted app loaded one nonblank `1098 x 718` canvas with no fresh console/page errors. The first app smoke caught a missing `getDimensionKeys` import and the slice was not marked complete until the fix was retested in app.
- [x] Stage 7G dimension-presentation canvas module split: added `wwwroot/canvas/dimensionPresentation.js`, moved dimension display text/labels, render styles, input CSS class construction, input refresh/commit predicates, and numeric formatting out of `drawingCanvas.js`, kept existing facade re-exports stable, and added `canvasDimensionPresentation.test.mjs`. JS presentation/target/dimension/geometry/canvas/dock suites passed 165/165, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the restarted app loaded one nonblank `1098 x 718` canvas with no fresh console/page errors. Remaining 7G dimension-rendering/input lifecycle, nearest-target hit-test, and tool-state module splits stay open.
- [x] Stage 7G target-classification canvas module split: added `wwwroot/canvas/targets.js`, moved target overlay filtering, point marker classification, dynamic target currency, middle-button pan detection, and constraint-tool target eligibility out of `drawingCanvas.js`, kept facade re-exports stable, and added `canvasTargets.test.mjs`. JS target/dimension/geometry/canvas/dock suites passed 160/160, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the restarted app loaded one nonblank `1098 x 718` canvas with no fresh console/page errors. Remaining 7G dimension-rendering/input, nearest-target hit-test, and tool-state module splits stay open.
- [x] Stage 7G dimension-geometry canvas module split: added `wwwroot/canvas/dimensions.js`, moved pure linear/radial dimension screen-geometry helpers out of `drawingCanvas.js`, kept facade re-exports stable, and added `canvasDimensions.test.mjs`. JS dimension/geometry/canvas/dock suites passed 155/155, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt app loaded a live nonblank canvas with no module-load console errors. Remaining 7G dimension-rendering/input, hit-test, and tool-state module splits stay open.
- [x] Stage 7G geometry-helper canvas module split: added `wwwroot/canvas/geometry.js`, moved pure world/screen geometry helpers out of `drawingCanvas.js`, kept `drawingCanvas.js` as the public facade, and added `canvasGeometry.test.mjs`. JS geometry/canvas/dock suites passed 153/153, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt app loaded a live nonblank canvas with no module-load console errors. Remaining 7G dimension/hit-test/tool-state module splits stay open in the Stage 7 plan.
- [x] Stage 7F trust/reference guardrails and import warnings: `DxfDocumentReader` now records unsupported entity counts and emits warning metadata, missing DXF units emit a warning, file open overlays source filename/hash metadata, inspector metadata exposes warnings/counts, `ApplyDocumentChange` blocks reference-only document mutation, and DWG selection shows an explicit external-viewer handoff status. Focused 7F tests passed 2/2, full core passed 424/424, `DXFER.slnx` rebuilt cleanly, and the rebuilt app verified unsupported DXF warning display plus DWG handoff behavior.
- [x] Stage 7E CAD IO adapter boundary: added `DXFER.CadIO`, moved `DxfDocumentReader` and `DxfDocumentWriter` out of `DXFER.Core`, updated Blazor/Web/test references, and added boundary tests proving Core no longer owns those DXF IO files. Focused CadIO/DXF tests passed 7/7, full core passed 422/422, `DXFER.slnx` rebuilt cleanly with 0 warnings/errors after killing the main web process, and the rebuilt app verified Save DXF still emits `Untitled.dxf` plus parseable `Untitled.dxfer.json`.
- [x] Stage 7D sidecar schema/export path: added `DxferSidecarDocument` and deterministic `DxferSidecarWriter` JSON with schema version, source/normalized filename and hashes, units, trust/reference mode, bounds, normalization counts, grain placeholder, warnings, and unsupported entity counts. `Save DXF` now emits both the normalized DXF and matching `.dxfer.json` sidecar. Focused sidecar/export tests passed 13/13, full core passed 419/419, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt app verified browser downloads for `Untitled.dxf` plus parseable `Untitled.dxfer.json`.
- [x] Stage 7C document metadata model: added immutable document metadata/warning records for source filenames and hashes, normalized filenames and hashes, units, trust/reference mode, warnings, and unsupported entity counts. Existing document constructors now default to empty metadata, and document-rebuild paths preserve metadata through workbench commits, selection deletion, split/modify/construction operations, sketch constraints, sketch dimensions, and geometry drag. Focused metadata tests passed 12/12, full core passed 411/411, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt app canvas smoke check verified one live nonblank canvas at `1098 x 718`.
- [x] Stage 7B2 entity-to-entity shortest distance measurement core coverage: added `MeasurementService.TryMeasureShortestDistance` with exact point/line/circle paths and segment-sampled fallback for other entities. Focused `MeasurementServiceTests` passed 14/14, full core passed 402/402, JS canvas plus dock passed 152/152, and `DXFER.slnx` rebuilt cleanly after killing the main web process. UI wiring for a shortest-distance command/readout remains a separate future slice.
- [x] Stage 7B1 scalar and bounds measurement coverage: added `docs/dev/measurement-coverage-matrix.md`, covered full polyline path length, arc length, sampled ellipse/spline length, polygon perimeter, point entity coordinates, entity bounds, and multi-selected entity bounds. Focused measurement tests passed 9/9, full core passed 396/396, JS canvas plus dock passed 152/152, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt app inspector verified selected arc measurement `X -5, Y 5, D 7.854` plus selected line/circle bounds `X 9, Y 4, D 9.849`.
- [x] Reopen parallel line-to-parallel line dimensioning: reproduced in the rebuilt app as midpoint snap clicks producing a midpoint-to-midpoint `lineardistance` dimension instead of a line-to-line `PointToLineDistance`. Added a failing JS regression for Dimension-tool midpoint target promotion, fixed `getDimensionSelectionKey` so line midpoints select the line and polyline midpoints select the segment, ran JS canvas plus dock tests 152/152, rebuilt `DXFER.slnx` cleanly after killing the main web process, and verified in the rebuilt running app that two separate toolbar-created parallel lines now produce a persistent `pointtolinedistance` input with value `4`.
- [x] Complete Stage 6 workspace UI and icon pass: dockable/free-floating tool groups were already integrated and verified; misleading green confirmed-working icon styling remains absent; explicit bad creation icons and polygon icons are corrected; and `docs/dev/tool-icon-audit-matrix.md` records the creation/toolbar icon review. Latest focused icon tests passed 5/5, full core passed 387/387, JS canvas plus dock passed 151/151, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt running app verified the corrected SVG geometry from the live DOM.
- [x] Stage 6b three-point arc icon slice: corrected the three-point arc icon so start, through, and end markers all sit on the displayed circular arc. Focused icon tests failed first, then passed 5/5; full core passed 387/387, JS canvas plus dock passed 151/151, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt running app verified the updated SVG arc marker distances from the live DOM.
- [x] Stage 6b polygon icon slice: corrected the inscribed polygon icon so its vertices lie on the guide circle, and corrected the circumscribed polygon icon so the guide circle is tangent to the polygon sides. Focused icon tests failed first, then passed 4/4; full core passed 386/386, JS canvas plus dock passed 151/151, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt running app verified the updated SVG distances from the live DOM.
- [x] Stage 6b icon slice for explicit bad creation icons: corrected the three-point circle icon so all three picked points sit on the circumference, and corrected the center-point arc icon so the arc is concentric with the displayed center. Focused icon tests failed first, then passed; full core passed 384/384, JS canvas plus dock passed 151/151, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt running app verified the updated SVG marker distances/path geometry.
- [x] Complete Stage 5 solver diagnostics rendering: added affected-reference diagnostics for unsatisfied dimensions/constraints, exposed dimension/constraint state and affected references through `CanvasDocumentDto`, and rendered affected geometry, dimension graphics/input text, and constraint glyphs in the red failed-state styling. Focused solver/DTO tests passed 2/2, full core passed 382/382, JS canvas plus dock passed 151/151, `DXFER.slnx` rebuilt cleanly after killing the main web process, and the rebuilt running app verified red affected geometry pixels, red dimension segment pixels, a red constraint glyph, and a red persistent dimension input class/color from an intentionally unsatisfied document.
- [x] Fix spline tangent handles: coded endpoint-only preview handles, exposed fit-point spline data to the canvas for committed entities, and rendered persistent endpoint handles after creation. Focused JS tests failed first on the missing persistent export, then passed; `CanvasDocumentDtoTests` passed 5/5, `DXFER.slnx` rebuilt cleanly after killing the running web process, and the rebuilt app canvas verified cyan handle pixels at both endpoint handle locations with zero cyan pixels at the old interior-handle locations.
- [x] Fix constraint selectability: coded canvas glyph hit testing so visible hover-only constraint glyphs can be selected even when `Show constraints` is off, kept hovered constraint targets from hiding their own glyph group, and excluded constraints from geometry overlay painting. Focused JS tests failed first, then passed; `DXFER.slnx` rebuilt cleanly after killing the running web process, and the rebuilt app verified a toolbar-created horizontal constraint selected as `constraint:horizontal-...` from the glyph with active selection matching the constraint key.
- [x] Fix three-point arc dimension input placement: reproduced the missing finite point coverage, then changed three-point arc preview dimensions to pass a real arc with valid start/end angles into the radius dimension helper. Focused JS failed first, then passed; `DXFER.slnx` rebuilt cleanly after killing the running web process, and the rebuilt app verified the radius input at `x=561.6, y=375.2` with value `4.167` during a three-point arc preview instead of the top-left.
- [x] Fix polygon tool previews: added a shared guide-circle helper and drew the radius/apothem guide circle before the polygon preview for both inscribed and circumscribed tools. Focused JS failed first on the missing helper export, then passed; `DXFER.slnx` rebuilt cleanly after killing the running web process, and the rebuilt app verified yellow guide-circle pixels for both `Inscribed polygon` and `Circumscribed polygon`.
- [x] Fix polygon auto-dimension placement: added a failing core regression proving polygon radius dimension anchors must be outside the picked radius/apothem point, then offset polygon radial anchors outward along the radial direction. Focused core failed first, then passed; `DXFER.slnx` rebuilt cleanly after killing the running web process, and the rebuilt app verified a typed polygon `R10` persistent input at `y=206`, outside the top radius point instead of clustered on the polygon corner.
- [x] Complete Stage 4 constraint glyph placement: added a failing JS regression proving default glyph rectangles must not contain their anchor point, then offset default glyph groups along a deterministic geometry normal while keeping leader lines only for manually dragged glyphs. Focused JS failed first, then passed; `DXFER.slnx` rebuilt cleanly after killing the running web process, and the rebuilt app verified a shared line vertex still hovered and selected as a `|point|end` target while constraint glyphs were visible.
- [x] Stage 0 status truth reset: coded, automated tests passed, verified in the running app that confirmed-green icon styling is absent.
- [x] Stage 1a Power Trim safety: coded, automated tests passed, verified in the running app that unsupported curve click and drag paths do not delete geometry. Functional curve trim support remains open above.
- [x] Stage 2 creation autoconstraints: coded, focused automated tests passed, verified in the running app for continued perpendicular lines and tangent-arc chain constraints.
- [x] Stage 3 ellipse drag behavior: coded, automated tests passed, verified in the running app for center, major-axis, and minor-axis drags.
- [x] Stage 3 dimension-respecting drag behavior: coded, automated tests passed, verified in the running app that dragging a dimensioned line endpoint preserved the driving dimension value and line bounds.
- [x] Stage 1b Power Trim circle and arc slice: coded, automated tests passed, verified in the running app that circle spans trim to arcs, line targets trim against circle cutters, and arc targets trim instead of blocking.
- [x] Stage 3b ellipse canvas behavior: coded, automated tests passed, verified in the running app that first-stage creation uses `Major diameter`, Enter advances to `Minor diameter`, ellipse edges hover as entity targets, and major/minor handle drags update inspector status/bounds. Screenshot capture timed out in the browser runtime, so verification used canvas data attributes and inspector state.
- [x] Stage 1c Power Trim line-target cutter matrix slice: coded line targets to accept line, polyline, polygon, circle, arc, ellipse, spline, and point cutters; focused and full core tests passed; running-app canvas verification passed for line, circle, arc, ellipse, polygon, spline, and point cutters. Polyline is covered by core tests, but app verification remains open because no direct workbench polyline creation path was found in this slice.
- [x] Stage 1d Power Trim ellipse-target line-cutter slice: coded ellipse targets trimmed by line cutters into elliptical arcs; full core and JS suites passed; verified in the running app that the picked ellipse span no longer hovers after trim while a kept span still hovers.
- [x] Stage 1e Power Trim spline-target line-cutter slice: coded spline targets trimmed by line cutters into kept spline spans; full core and JS suites passed; verified in the running app that the picked spline span no longer hovers after trim while a kept span still hovers.
- [x] Stage 1f Power Trim arc-target arc-cutter slice: coded arc targets trimmed by arc cutters, including tangent/touch cutter points that do not visually cross; `DrawingModifyServiceTests` passed 26/26, full core passed 267/267, JS canvas suite passed 122/122, and running-app canvas verification passed for both crossing arc cutters and tangent/touch arc cutter plus line cutter.
- [x] Stage 1g Power Trim circle-target cutter matrix slice: coded circle targets trimmed by circle, arc, polyline, polygon, ellipse, spline, and point cutters, including tangent/touch arc cutter points; `DrawingModifyServiceTests` passed 34/34, full core passed 275/275, JS canvas suite passed 122/122, and running-app canvas verification passed for line, circle, arc, ellipse, polygon, spline, and point cutters. Polyline remains core-only until loaded/imported polyline canvas verification.
- [x] Stage 1h Power Trim arc-target remaining-cutter matrix slice: coded arc targets trimmed by polyline, polygon, ellipse, spline, and point cutters; `DrawingModifyServiceTests` passed 39/39, full core passed 280/280, JS canvas suite passed 122/122, and running-app canvas verification passed for ellipse, polygon, spline, and point cutters. Polyline remains core-only until loaded/imported polyline canvas verification.
- [x] Stage 1i Power Trim ellipse-target remaining-cutter matrix slice: coded ellipse targets trimmed by circle, arc, polyline, polygon, ellipse, spline, and point cutters; `DrawingModifyServiceTests` passed 46/46, full core passed 287/287, JS canvas suite passed 122/122, and running-app canvas verification passed for circle, arc, ellipse, polygon, spline, and point cutters. Polyline remains core-only until loaded/imported polyline canvas verification.
- [x] Stage 1j Power Trim spline-target remaining-cutter matrix slice: coded spline targets trimmed by circle, arc, polyline, polygon, ellipse, spline, and point cutters, including tangent/touch arc cutter points; `DrawingModifyServiceTests` passed 54/54, full core passed 295/295, JS canvas suite passed 122/122, `DXFER.slnx` rebuilt cleanly, and running-app canvas verification passed for line, circle, arc, ellipse, polygon, spline, and point cutters plus tangent/touch arc. Polyline remains core-only until loaded/imported polyline canvas verification.
- [x] Reopen Power Trim functional support: implemented actual trim behavior beyond line-only safety for circles, arcs, splines, and ellipses; each curve target has focused core coverage and running-app verification for UI-creatable cutters. Broader trim completion remains open in the applicability matrix item.
- [x] Stage 1k reopened ellipse/spline trim quality fixes: coded and tested near-edge ellipse pick handling, projected Power Trim canvas points, and non-reinterpolated trimmed spline spans. `DrawingModifyServiceTests` passed 64/64, full core passed 305/305, JS canvas suite passed 123/123, `DXFER.slnx` rebuilt cleanly, and running-app canvas verification passed for off-edge ellipse target trimming plus curved spline target trimming.
- [x] Rerun full ellipse and spline target functional trim rows after latest quality fixes: running-app checks passed for ellipse targets with line, circle, arc, ellipse, polygon, spline, and point cutters, and spline targets with line, circle, arc, ellipse, polygon, spline, and point cutters. Spline endpoint precision remains open above because the rerun checked functional trimming and hover behavior, not exact geometric coincidence.
- [x] Stage 1l spline precision slice: added failing endpoint-coincidence tests and refined Catmull-Rom intersections for line, circle, arc, and ellipse targets using curved spline cutters, plus spline targets using circle cutters. `DrawingModifyServiceTests` passed 69/69, full core passed 310/310, JS canvas suite passed 123/123, `DXFER.slnx` rebuilt cleanly, and running-app checks passed for circle target + curved spline cutter, ellipse target + curved spline cutter, and spline target + circle cutter.
- [x] Stage 1m spline target precision expansion: added failing endpoint-coincidence tests for spline targets trimmed by line cutters and line-segment spline cutters, then refined those intersections to exact Catmull-Rom target roots. `DrawingModifyServiceTests` passed 73/73, full core passed 314/314, JS canvas suite passed 123/123, `DXFER.slnx` rebuilt cleanly, and rebuilt-app checks passed for spline targets with line, arc, ellipse, and line-segment spline cutters plus ellipse targets with a crossing spline cutter.
- [x] Stage 1n curved spline-vs-spline trim precision: added a failing endpoint-coincidence test for a spline target trimmed by curved spline cutters, then refined fit-point spline/spline intersections with Catmull-Rom curve-curve solving. Focused tests passed 3/3, `DrawingModifyServiceTests` passed 74/74, full core passed 315/315, JS canvas suite passed 123/123, `DXFER.slnx` rebuilt cleanly, and rebuilt-app canvas verification changed entity count `3 -> 4` with the picked middle span removed and both kept endpoint spans hoverable.
- [x] Stage 1o polygon and point trim target support: implemented polygon targets as closed-perimeter trims that keep an open polyline remainder, and point targets as deliberate point deletion. Polygon core coverage includes line, circle, arc, ellipse, polyline, polygon, spline, and point cutters; point core coverage verifies deletion. `DrawingModifyServiceTests` passed 83/83, full core passed 324/324, JS canvas suite passed 123/123, `DXFER.slnx` rebuilt cleanly, and rebuilt-app verification passed for polygon target plus line cutter and point target deletion.
- [x] Stage 1p Power Trim line extend/trim disambiguation: reproduced the canvas request boundary where an off-end line click inside edge-hit tolerance snapped back to the endpoint; added JS regression coverage and preserved the raw off-end pick for line extension requests. Added core coverage for line end extension to a line boundary. Focused core tests passed 2/2, JS canvas suite passed 124/124, `DXFER.slnx` rebuilt cleanly, and rebuilt-app verification extended the target endpoint from `10,0` to `15,0` without trimming or deleting the source line.
- [x] Stage 1q ellipse target hit priority: reproduced the canvas hit-test bug where a line cutter endpoint coincident with a non-quadrant ellipse edge targeted the cutter line instead of the ellipse. Power Trim now prefers edge targets over coincident line/polyline/polygon point hits for its own request/hover path. JS canvas suite passed 125/125, `DXFER.slnx` rebuilt cleanly, and rebuilt-app verification passed for the coincident line-endpoint case plus the ellipse target row with line, circle, arc, ellipse, polygon, spline, and point cutters.
- [x] Stage 1r control-point spline trim precision: reproduced sampled-intersection leakage for non-fit/control-point splines as both cutters and targets. Added red-green endpoint tests for line targets cut by control-point splines and control-point spline targets cut by lines; refined knot-parameter spline intersections for line/circle/arc/ellipse target and cutter paths. Full core passed 327/327, JS canvas suite passed 125/125, `DXFER.slnx` rebuilt cleanly, and rebuilt-app canvas verification passed for a control-point spline cutter trimming a line with `0` pixel endpoint error plus a control-point spline target split by line cutters.
- [x] Stage 1s Power Trim open-entity no-cutter deletion: restored direct Power Trim deletion for open polylines, arcs, elliptical arcs, and splines when the picked section has no cutters, while full ellipses remain unchanged. Focused no-cutter tests passed 4/4, PowerTrim core passed 85/85, full core passed 331/331, JS canvas suite passed 125/125, `DXFER.slnx` rebuilt cleanly after killing the locked web process, and rebuilt-app canvas verification passed for open spline, open arc, and open elliptical arc deletion plus closed ellipse unchanged behavior.
- [x] Stage 1t sample polyline canvas verification slice: used the built-in sample document's `bend-path` polyline to verify previously core-only polyline canvas paths. Rebuilt-app evidence passed for polyline target trimmed by line cutters plus line, circle, arc, and ellipse targets trimmed by a polyline cutter. Broader polyline canvas coverage remains open in the matrix.
- [x] Stage 1u line extend boundary coverage: added focused core coverage for extending line targets to line, polyline, polygon, circle, arc, ellipse, spline, and point boundaries. Focused boundary tests passed 7/7, PowerTrim core passed 92/92, `DXFER.slnx` rebuilt cleanly, and rebuilt-app canvas verification passed for circle, arc, ellipse, spline, and point boundaries. Polyline and polygon boundary app verification remains open in the matrix.
- [x] Stage 1v open-polyline extend boundary coverage: added red-green core tests for extending open polyline start/end points to line, polyline, polygon, circle, arc, ellipse, spline, and point boundaries, plus JS coverage that preserves raw off-end polyline Power Trim picks. Focused polyline extend tests passed 8/8, PowerTrim core passed 100/100, JS canvas tests passed 126/126, full core passed 346/346, `DXFER.slnx` rebuilt cleanly, rebuilt browser-canvas verification confirmed a shipped canvas instance sent `OnPowerTrimRequested` with raw point `10.2000003,0` instead of the clamped endpoint, and full workbench canvas verification passed for the sample `bend-path` polyline extending to a line boundary.
- [x] App menu command routing blocker: removed the duplicate page-level interactive render boundary and made externally invoked workbench commands call `StateHasChanged`, so header menu commands update the active workbench. Focused render/menu tests passed 3/3, `DXFER.slnx` rebuilt cleanly, and rebuilt-app verification loaded `Open sample` to 7 entities with bounds `120 x 80`.
- [x] Stage 1w open-spline extend boundary coverage: added red-green core tests for extending open spline start/end points to line, polyline, polygon, circle, arc, ellipse, spline, and point boundaries, plus JS coverage that preserves raw off-end spline Power Trim picks. Focused spline extend tests passed 8/8, PowerTrim core passed 108/108, JS canvas tests passed 127/127, `DXFER.slnx` rebuilt cleanly, and rebuilt browser-canvas verification confirmed a shipped canvas instance sent `OnPowerTrimRequested` with raw point `10.2,0` instead of the clamped endpoint.
- [x] Stage 1x open-arc and open-elliptical-arc extend boundary coverage: added red-green core tests for extending arc and elliptical arc start/end points to line, polyline, polygon, circle, arc, ellipse, spline, and point boundaries, plus JS coverage that preserves raw off-end arc and elliptical arc Power Trim picks. Focused arc/ellipse-arc extend tests passed 18/18 after failing first, PowerTrim core passed 126/126, JS canvas tests passed 129/129, full core passed 373/373, `DXFER.slnx` rebuilt cleanly, and rebuilt browser-canvas verification confirmed a shipped canvas instance sent raw off-end points for both arc and elliptical arc requests instead of the clamped endpoints.
- [x] Stage 1y closed/non-open extend target classification: marked polygon, circle, full ellipse, and point extend-target rows as deliberate `N/A` in the matrix because they have no open endpoint/span to extend. Added canvas request tests proving closed circle and polygon near-edge Power Trim picks project to trim points instead of raw off-end extension picks; full ellipse projection and point trim/delete behavior were already covered. JS canvas tests passed 131/131.
- [x] Stage 1z open arc and elliptical-arc full workbench line-boundary extend verification: rebuilt and restarted the app, created the target and boundary entities through the toolbar, clicked just past the open endpoint with Power Trim, and verified in the real canvas that entity count stayed `2` while a previously unhoverable span became hoverable as the extended arc or elliptical arc.
- [x] Stage 1aa open spline full workbench line-boundary extend verification: created the spline target and line boundary through the toolbar, clicked just past the spline endpoint with Power Trim, and verified in the real canvas that entity count stayed `2` while a probe past the old endpoint became hoverable as the extended spline.
- [x] Stage 1ab line target full workbench polyline-boundary extend verification: loaded the sample document for a real `bend-path` polyline boundary, added a target line through the toolbar, clicked just past the line endpoint with Power Trim, and verified in the real canvas that entity count stayed stable while a probe between the old endpoint and polyline boundary became hoverable as the extended line.
- [x] Stage 1ac line target full workbench polygon-boundary extend verification: created the target line and an inscribed polygon boundary through the toolbar, clicked just past the line endpoint with Power Trim, and verified in the real canvas that entity count stayed `2` while the probe between the old endpoint and polygon boundary became hoverable as the extended line.
- [x] Stage 1ad open polyline target full workbench circle-boundary extend verification: loaded the sample `bend-path` polyline target, created a circle boundary on its endpoint extension ray, clicked just past the polyline endpoint with Power Trim, and verified in the real canvas that entity count stayed `8` while the probe beyond the old endpoint became hoverable as `bend-path|segment|4`.
- [x] Stage 1ae open polyline target full workbench point-boundary extend verification: loaded the sample `bend-path` polyline target, created a point boundary on its endpoint extension ray, clicked just past the polyline endpoint with Power Trim, and verified in the real canvas that entity count stayed `8` while the probe beyond the old endpoint became hoverable as `bend-path|segment|4`.
- [x] Stage 1af open polyline target full workbench spline-boundary extend verification: loaded the sample `bend-path` polyline target, created a spline boundary crossing its endpoint extension ray, clicked just past the polyline endpoint with Power Trim, and verified in the real canvas that entity count stayed `8` while the probe beyond the old endpoint became hoverable as `bend-path|segment|4`.
- [x] Stage 1ag open polyline target full workbench arc-boundary extend verification: loaded the sample `bend-path` polyline target, created a center-point arc boundary crossing its endpoint extension ray, clicked just past the polyline endpoint with Power Trim, and verified in the real canvas that entity count stayed `8` while the probe beyond the old endpoint became hoverable as `bend-path|segment|4`.
- [x] Stage 1ah open polyline target full workbench ellipse-boundary extend verification: loaded the sample `bend-path` polyline target, created an ellipse boundary with its major axis on the endpoint extension ray, clicked just past the polyline endpoint with Power Trim, and verified in the real canvas that entity count stayed `8` while the probe beyond the old endpoint became hoverable as `bend-path|segment|4`.
- [x] Stage 1ai open polyline target full workbench polygon-boundary extend verification: loaded the sample `bend-path` polyline target, created an inscribed polygon boundary on its endpoint extension ray, clicked just past the polyline endpoint with Power Trim, and verified in the real canvas that entity count stayed `8` while the probe beyond the old endpoint became hoverable as `bend-path|segment|4`.
- [x] Stage 1aj open spline target full workbench boundary-row verification: created spline targets through the toolbar and verified extension against circle, polygon, arc, ellipse, spline, and point boundaries; loaded the sample `bend-path` polyline for the polyline boundary cell. Every case kept entity count stable and changed the probe beyond the old spline endpoint to the extended spline entity.
- [x] Stage 1ak open arc target full workbench boundary-row verification: created arc targets through the toolbar and verified extension against circle, polygon, arc, ellipse, spline, and point boundaries; loaded the sample `bend-path` polyline for the polyline boundary cell. Every case kept entity count stable and changed the probe beyond the old arc endpoint to the extended arc entity.
- [x] Stage 1al open elliptical-arc target full workbench boundary-row verification: created elliptical-arc targets through the toolbar and verified extension against circle, polygon, arc, ellipse, spline, and point boundaries; loaded the sample `bend-path` polyline for the polyline boundary cell. Every case kept entity count stable and changed the probe beyond the old elliptical-arc endpoint to the extended elliptical arc entity.
- [x] Stage 1am open polyline target full workbench polyline-boundary extend verification and header file-open render fix: added a focused render guard and made `OpenFileAsync` schedule `StateHasChanged` after header file-open requests. Rebuilt the app, imported a two-polyline DXF through the visible file chooser, clicked just past the target polyline endpoint, and verified entity count stayed `2` while the probe at `12,0` became hoverable as `polyline-1|segment|2`.
- [x] Expand Power Trim applicability matrix: evaluated line, polyline, polygon, circle, arc, ellipse, spline, and point as trim cutters/targets and extend targets/boundaries where geometrically applicable. Focused Power Trim tests passed 126/126, canvas tests passed 128/128, and running-app verification completed the trim matrix plus the extend matrix.
- [x] Keep Power Trim and Extend matrix tracking current in `docs/dev/power-trim-extend-test-matrix.md`: every row/column cell is now marked `CORE+APP` or deliberate `N/A`; no `TODO`, `OPEN`, or `CORE`-only cells remain in Stage 1.
- [x] Subagent dockable tool panels request: integrated independent dockable/free-floating tool groups from the isolated worktree without restoring confirmed-green icon styling. `dotnet build .\DXFER.slnx --no-restore` passed, `workbenchDockModel.test.mjs` passed 13/13, `drawingCanvasSelection.test.mjs` passed 128/128, `WorkbenchRenderBoundaryTests` passed 2/2, and the rebuilt running app at `http://127.0.0.1:5119` was browser-verified for floating plus left, right, top, and bottom docking; bottom stayed above the command/status panel, right stayed left of the inspector, and `.dxfer-icon-button-confirmed` count was `0`.
- [x] Implement dimensioning from one parallel line to another parallel line: coded parallel line and polyline-segment selections to create `PointToLineDistance` dimensions instead of zero angle dimensions; coded solver support for driving line/segment offset distance; coded canvas rendering for line-line distance dimensions. Focused tests failed first, then passed; full core tests passed 378/378, JS canvas plus dock tests passed 142/142, `DXFER.slnx` rebuilt cleanly, and the rebuilt running app was verified by creating two toolbar lines, applying a line-to-line dimension, and observing a persistent `pointtolinedistance` input with positive displayed value.
